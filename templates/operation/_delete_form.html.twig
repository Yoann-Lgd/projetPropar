<form method="post" action="{{ path('operation_delete', {'id': operation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cette opération ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operation.id) }}">
    <button class="btn btn-danger text-white rounded-custom px-3 fw-bold">Supprimer</button>
</form>
