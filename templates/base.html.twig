<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link
		rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
		<style>
			html {
				background: linear-gradient(0deg, rgba(32, 212, 137, 0.75), rgba(32, 212, 137, 0.75)), url({{ asset('build/images/BaseBackground.png') }});
				background-repeat: no-repeat;
				background-position: center;
				background-attachment: fixed;
				margin: 0;
				padding: 0;
				height: 100%;

			}
			body {
				height: 100%;
				display: flex;
				font-family: Roboto;
				background: none;
				/* display: flex;
            flex-direction: row;
            justify-content: flex-end;
            align-items: center;
            padding: 0px;
            position: relative;
            font-family: Roboto;
            background: none;*/
			}

			p {
				margin: 0;
			}
			#baseSidebar {
				min-width: 308px;
			}
			#baseSidebarHeader {
				/*display: flex;
            flex-direction: row;
            justify-content: center;
            align-items: center;
            padding: 10px;
            align-self: stretch;
            height: 92px;*/
			}
			#baseSidebarHeader > h3 {

				/* font-style: normal;
            font-weight: 500;
            font-size: 18px;
            line-height: 21px;

            color: #FFFFFF;*/
			}
			#baseSidebarBody {
				/*display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px;
            align-self: stretch;
            height: 100%;*/
			}

			#baseBody {
				width: 100%;
			}
			#navbar {
				height: 92px
			}

			#container {
				height: 100%;
			}
			/*  #baseBodyDiv {
            background-color: #F5F8FA; 
            height: 100%;
            flex: 3;
        }

        
        #navbar > div {
            display: flex;
            flex-direction: row;
            justify-content: end;
            align-items: center;
            width: 100%;
            text-align: center;
            height: 100%;
        }
        #navbar > div > p {
            margin: 0;
        }
        #navbar > div > img {
            border: 2px solid #20D489;
            box-sizing: border-box;
            border-radius: 5px; 
            margin-left: 10px;
        }
        #container {
            display: flex;
            flex-direction: column;
            align-items: left;
            padding: 40px;

            height: auto;

            background: #F5F8FA;
        }*/
        .shadow-custom {
            box-shadow: 0px 2px 28px rgba(58, 59, 69, 0.01);
        }
        .rounded-custom {
            border-radius: 10px;
        }
		</style>
	</head>
	<body>
		<div id="baseSidebar" class="d-flex flex-column align-items-center ">
			<div id="baseSidebarHeader">
				<h3 class="text-white">
					Projet Propar
				</h3>
			</div>
			<div id="baseSidebarBody"></div>
		</div>
		<div id="baseBody" class="bg-light d-flex flex-column">

			<div id="navbar" class="d-flex flex-row align-items-center justify-content-between sticky-top px-5"> {% block search %}{% endblock %}
				<div class="d-flex flex-row align-items-center">
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<p class="text-black-50 px-3">{{ app.user.nom }}
							{{ app.user.prenom }}</p>
						<img src="https://avatars.dicebear.com/api/initials/{{ app.user.nom }}.{{ app.user.prenom }}.svg" height="32" width="32" alt="..." class="border border-secondary border-2 rounded-2 ">
					{% endif %}
				</div>
			</div>

			<div id="container" class="d-flex flex-column p-5"> {% block body %}{% endblock %}
				</div>
			</div>

		</body>
	</html>
